// SPDX-License-Identifier: AGPL-3.0-or-later
import{describe,expect,it}from'vitest';import corpus from'../../../../mb-printer-sdk/fixtures/template/corpus.json';import{evaluateTemplate,mapCsvFields,parseCsv}from'../src/index.js';
describe('shared SDK template parity',()=>{it('parses quoted CSV and explicit field maps',()=>{const parsed=parseCsv('source,city\n"Doe, Jane",Paris\n');expect(parsed).toEqual({fields:['source','city'],records:[{source:'Doe, Jane',city:'Paris'}]});expect(mapCsvFields(parsed.records,{name:'source',place:'city'})).toEqual([{name:'Doe, Jane',place:'Paris'}])});it.each(corpus.cases)('$name matches the canonical SDK corpus',entry=>{const run=()=>evaluateTemplate(entry.template,{record:entry.fields as Record<string,string>,locale:entry.locale,currentDate:entry.date});if('error'in entry)expect(run).toThrow(entry.error);else expect(run()).toBe(entry.output)});it('rejects transforms outside the allowlist',()=>{expect(()=>evaluateTemplate('{{name|eval}}',{record:{name:'Ada'}})).toThrow('unknown transform: eval')})});
