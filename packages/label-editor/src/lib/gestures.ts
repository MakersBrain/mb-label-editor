// SPDX-License-Identifier: AGPL-3.0-or-later
import type{Point}from'./model.js';
export interface GestureUpdate{panDelta:Point;zoomFactor:number;center:Point}
export class GestureTracker{#points=new Map<number,Point>();#previous?:{center:Point;distance:number};start(id:number,point:Point){this.#points.set(id,point);this.#previous=this.measure()}move(id:number,point:Point):GestureUpdate|undefined{if(!this.#points.has(id))return;const before=this.#previous;this.#points.set(id,point);const after=this.measure();this.#previous=after;if(!before||!after)return;return{panDelta:{x:after.center.x-before.center.x,y:after.center.y-before.center.y},zoomFactor:before.distance&&after.distance?after.distance/before.distance:1,center:after.center}}end(id:number){this.#points.delete(id);this.#previous=this.measure()}get active(){return this.#points.size}private measure(){const values=[...this.#points.values()];if(!values.length)return undefined;if(values.length===1)return{center:values[0],distance:0};const[a,b]=values;return{center:{x:(a.x+b.x)/2,y:(a.y+b.y)/2},distance:Math.hypot(b.x-a.x,b.y-a.y)}}}
