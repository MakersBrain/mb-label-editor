// SPDX-License-Identifier: AGPL-3.0-or-later
import {sha256} from './files.js';import {uuid,type FontResource,type Resource} from './model.js';import type {PrinterSdk} from './print/types.js';
const base64=(bytes:Uint8Array)=>{let binary='';for(let offset=0;offset<bytes.length;offset+=0x8000)binary+=String.fromCharCode(...bytes.subarray(offset,offset+0x8000));return btoa(binary)};
const bytes=(buffer:ArrayBuffer)=>new Uint8Array(buffer.slice(0));
export async function importAsset(file:File,sdk?:PrinterSdk,dpi=300):Promise<{resource:Resource;widthMm?:number;heightMm?:number}> {let data:Uint8Array=bytes(await file.arrayBuffer());let mimeType=file.type||mimeFromName(file.name);let widthMm:number|undefined;let heightMm:number|undefined;if(mimeType==='application/pdf'){if(!sdk)throw new Error('Offline PDF import requires the installed printer SDK.');const page=await sdk.importFirstPdfPage(data,dpi);data=page.data;mimeType=page.mimeType;widthMm=page.widthMm;heightMm=page.heightMm}if(mimeType==='image/svg+xml'){const source=new TextDecoder().decode(data);if(/(?:href|src)\s*=\s*["'](?:https?:|\/\/)/i.test(source)||/<script\b/i.test(source))throw new Error('SVG must not contain scripts or external resources.')}if(!mimeType.startsWith('image/'))throw new Error(`Unsupported asset type: ${mimeType}`);const digest=await sha256(new Uint8Array(data).buffer);return{resource:{id:uuid(),name:file.name.replace(/\.pdf$/i,'.png'),mimeType,sha256:digest,data:base64(data)},widthMm,heightMm}}
export async function importFont(file:File,details?:{family?:string;weight?:number;style?:'normal'|'italic'}):Promise<FontResource>{const data=bytes(await file.arrayBuffer());const mimeType=file.type||mimeFromName(file.name);if(!['font/woff','font/woff2','font/ttf','font/otf','application/font-sfnt'].includes(mimeType))throw new Error(`Unsupported font type: ${mimeType}`);return{id:uuid(),name:file.name,mimeType,sha256:await sha256(new Uint8Array(data).buffer),data:base64(data),family:details?.family??file.name.replace(/\.[^.]+$/,''),weight:details?.weight??400,style:details?.style??'normal'}}
const mimeFromName=(name:string)=>{const extension=name.split('.').pop()?.toLowerCase();return({svg:'image/svg+xml',png:'image/png',jpg:'image/jpeg',jpeg:'image/jpeg',webp:'image/webp',pdf:'application/pdf',woff:'font/woff',woff2:'font/woff2',ttf:'font/ttf',otf:'font/otf'} as Record<string,string>)[extension??'']??'application/octet-stream'};
