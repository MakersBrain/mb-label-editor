// SPDX-License-Identifier: AGPL-3.0-or-later
import{uuid,type LabelDocument}from'./model.js';import{EditorDatabase,type PersistedJob}from'./persistence/database.js';import type{PrintProgress,PrintResult}from'./print/types.js';
export class JobJournal{constructor(private database:EditorDatabase){}async save(job:PersistedJob){await this.database.saveJob(job);return job}async begin(document:LabelDocument,route:string,id=uuid(),snapshot?:unknown){return await this.save({id,documentId:document.id,createdAt:new Date().toISOString(),state:'planned',route,resumable:true,details:{lastCompletedAction:-1,bytesSent:0,outcome:'not-started',...(snapshot===undefined?{}:{snapshot})}})}async progress(job:PersistedJob,value:PrintProgress){const next={...job,state:value.bytesSent?'sending':'planned',resumable:true,details:{...details(job.details),...value,outcome:value.bytesSent?'potentially-accepted':'not-started'}};return await this.save(next)}async finish(job:PersistedJob,result:PrintResult){const ambiguous=['outcome-unknown','cancelled-partial'].includes(result.outcome);const next={...job,state:result.outcome,resumable:ambiguous,details:{...details(job.details),...result}};return await this.save(next)}async recover(){return(await this.database.entries<PersistedJob>('jobs')).map(item=>item.value).filter(job=>job.resumable).sort((a,b)=>b.createdAt.localeCompare(a.createdAt))}async recent(route?:string){return(await this.database.entries<PersistedJob>('jobs')).map(item=>item.value).filter(job=>!route||job.route===route).sort((a,b)=>b.createdAt.localeCompare(a.createdAt))}}
const details=(value:unknown):Record<string,unknown>=>value&&typeof value==='object'&&!Array.isArray(value)?value as Record<string,unknown>:{};
