// SPDX-License-Identifier: AGPL-3.0-or-later
import{execFile}from'node:child_process';import{mkdtemp,mkdir,readFile,rm,symlink,writeFile}from'node:fs/promises';import{join}from'node:path';import{promisify}from'node:util';import{fileURLToPath}from'node:url';import{build}from'vite';import{svelte}from'@sveltejs/vite-plugin-svelte';const exec=promisify(execFile);const root=fileURLToPath(new URL('../',import.meta.url));const temporary=await mkdtemp(join(root,'.package-consumer-'));try{const modules=join(temporary,'node_modules');const scope=join(modules,'@makersbrain');await mkdir(scope,{recursive:true});let archive=process.env.PACKAGE_TARBALL;if(!archive){const{stdout}=await exec('npm',['pack','--json','--pack-destination',temporary],{cwd:join(root,'packages/label-editor'),env:{...process.env,npm_config_cache:join(temporary,'cache')}});archive=join(temporary,JSON.parse(stdout)[0].filename)}const output=join(scope,'label-editor');await mkdir(output);await exec('tar',['-xzf',archive,'-C',output,'--strip-components=1']);for(const dependency of['svelte'])await symlink(join(root,'node_modules',dependency),join(modules,dependency),'dir');const manifest=JSON.parse(await readFile(join(output,'package.json'),'utf8'));if(manifest.types!=='./dist/index.d.ts'||manifest.exports?.['.']?.svelte!=='./dist/index.js'||manifest.publishConfig?.registry!=='https://registry.npmjs.org/')throw new Error('packed package entrypoints or registry are incomplete');for(const name of['LICENSE','README.md','THIRD_PARTY_NOTICES.md'])await readFile(join(output,name),'utf8');await writeFile(join(temporary,'index.html'),'<div id="app"></div><script type="module" src="/main.js"></script>');await writeFile(join(temporary,'main.js'),"import{mount}from'svelte';import{LibraryPanel,createEditorStore,defaultDocument}from'@makersbrain/label-editor';mount(LibraryPanel,{target:document.querySelector('#app'),props:{editor:createEditorStore(defaultDocument())}});");await build({root:temporary,logLevel:'silent',plugins:[svelte()],build:{outDir:join(temporary,'consumer-dist')}});await readFile(join(temporary,'consumer-dist','index.html'),'utf8');console.log('clean packed-package Svelte consumer build passed')}finally{await rm(temporary,{recursive:true,force:true})}
