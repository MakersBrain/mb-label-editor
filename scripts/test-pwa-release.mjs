// SPDX-License-Identifier: AGPL-3.0-or-later
import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join,normalize}from'node:path';import{fileURLToPath}from'node:url';import{chromium}from'playwright';const root=fileURLToPath(new URL('../',import.meta.url));const dist=join(root,'apps/pwa/dist');const prefix='/mb-label-editor/';const types={'.html':'text/html','.js':'text/javascript','.css':'text/css','.wasm':'application/wasm','.webmanifest':'application/manifest+json','.svg':'image/svg+xml'};const server=createServer(async(request,response)=>{try{const url=new URL(request.url??'/',`http://${request.headers.host}`);if(!url.pathname.startsWith(prefix)){response.writeHead(404).end();return}const relative=url.pathname.slice(prefix.length)||'index.html';let path=join(dist,normalize(relative));if(!(await stat(path)).isFile())path=join(dist,'index.html');response.setHeader('content-type',types[extname(path)]??'application/octet-stream');response.end(await readFile(path))}catch{response.writeHead(404).end()}});await new Promise(resolve=>server.listen(0,'127.0.0.1',resolve));const address=server.address();if(!address||typeof address==='string')throw new Error('release PWA server did not bind');const browser=await chromium.launch({headless:true,...(process.env.CHROMIUM_PATH?{executablePath:process.env.CHROMIUM_PATH}:{}),args:['--no-sandbox']});try{const context=await browser.newContext();const page=await context.newPage();const url=`http://127.0.0.1:${address.port}${prefix}`;await page.goto(url);if(await page.title()!=='MakersBrain Label Editor')throw new Error('non-root PWA failed to load');const manifest=await page.locator('link[rel=manifest]').getAttribute('href');const manifestResponse=await context.request.get(new URL(manifest??'',url).href);if(!manifestResponse.ok())throw new Error('non-root manifest failed');const wasmResponse=await context.request.get(new URL((await page.locator('script[type=module]').getAttribute('src'))??'',url).href).catch(()=>undefined);if(wasmResponse&&!wasmResponse.ok())throw new Error('non-root module failed');await page.evaluate(()=>navigator.serviceWorker.ready);await context.setOffline(true);await page.reload();if(await page.title()!=='MakersBrain Label Editor')throw new Error('installed non-root PWA failed offline reload');await context.close();console.log('non-root hosted PWA and offline service-worker acceptance passed')}finally{await browser.close();server.close()}
